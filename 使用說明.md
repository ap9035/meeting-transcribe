# 會議逐字稿工具（本機離線版）

把錄音檔轉成**帶講者標記和時間戳**的逐字稿。全部在自己的電腦上跑，錄音不會上傳到任何地方。

```
[00:00:00] 講者1：各位早安，今天我們要討論第三季的預算分配。

[00:00:06] 講者2：好的，我這邊先報告目前的執行狀況。
```

---

# 第一部分：安裝（只要做這一次，之後都不用再看）

安裝要在一個叫「終端機」的黑底白字視窗裡打幾行字。看起來很嚇人，但其實只是**複製、貼上、按 Enter**，
下面每一步都寫好可以直接複製的內容，照著做就好。全程約 20～40 分鐘，需要保持連網。

> 打錯字不會弄壞電腦。畫面上跑出一大堆看不懂的英文是正常的，不用管它。

## 步驟 1：打開「終端機」

1. 按鍵盤上的 **Command（⌘）+ 空白鍵**，畫面正中間會跳出一個搜尋框。
2. 輸入「**終端機**」。
3. 第一個結果就是「終端機」，按 **Enter**。

會跳出一個白色或黑色的視窗，裡面有一行字，最後有個游標在閃。這個視窗接下來都不要關掉。

## 步驟 2：安裝 git（下載工具用的）

在終端機視窗裡貼上這一行，然後按 Enter：

```
git --version
```

> 貼上的方法：把上面那行選起來、按 ⌘+C 複製，點一下終端機視窗，按 ⌘+V 貼上，再按 Enter。

接下來會是下面兩種情況之一：

**情況 A：跳出一個視窗問你要不要安裝「命令列開發者工具」**

表示這台電腦還沒有 git。按視窗上的「**安裝**」，再按「**同意**」。
然後就是等——會下載幾百 MB，快的話 5 分鐘，慢的話 20 分鐘以上，中間看起來沒動靜是正常的。
（這是 Apple 官方的工具包，不是來路不明的東西。）

等它說「軟體已安裝」之後，回到終端機，**再貼一次同一行**：

```
git --version
```

這次應該會印出像 `git version 2.39.5` 這樣的一行字，就代表好了。

**情況 B：直接印出 `git version 2.39.5` 之類的字**

太好了，這台電腦本來就有 git，什麼都不用做，直接跳到步驟 3。

> 如果情況 A 的視窗不小心被你按到「不是現在」關掉了，沒關係，
> 在終端機貼上 `xcode-select --install` 按 Enter，視窗就會再出現一次。

## 步驟 3：下載工具並開始安裝

確認上一步有印出 `git version …` 之後，在終端機貼上**下面這一整段**（是一行，很長，整段複製），按 Enter：

```
cd ~/Documents && git clone https://github.com/ap9035/meeting-transcribe.git && bash meeting-transcribe/安裝.command
```

這行做了三件事：跑到「文件」資料夾 → 把工具下載下來 → 直接開始安裝。

> **為什麼不能用瀏覽器下載 ZIP？**
> 從網頁下載或用 AirDrop 傳來的檔案，會被 macOS 貼上「隔離」標記，雙擊時會被擋住說「來自未識別的開發者」。
> 用上面這行指令抓下來的沒有這個問題，而且之後的「🔄 更新工具」也只有這樣抓下來才能運作。

## 步驟 4：照著畫面走完安裝

安裝程式會自己跑，中間會停下來問你一次**授權碼**——這是用來辨識「誰在說話」的模型需要的，免費，只要辦一次。
取得方式（約 3 分鐘，可以先開瀏覽器把三個網頁都弄好，再回來貼）：

1. 到 <https://huggingface.co/join> 註冊一個免費帳號（用 email 就可以）
2. 到 <https://huggingface.co/pyannote/speaker-diarization-community-1>
   把頁面上的使用條款按「**Agree** / **Accept**」同意
3. 到 <https://huggingface.co/settings/tokens>
   按「**New token**」，Type 選「**Read**」，建立完複製那串 `hf_` 開頭的字

回到終端機，貼在「授權碼：」後面按 Enter。

> 直接按 Enter 跳過也可以，逐字稿一樣做得出來，只是不會標「講者1、講者2」。
> 之後想補設定，重跑一次「安裝.command」就好。

剩下的就是等下載模型（約 1.5GB）。跑完會說「安裝完成 🎉」，這時候就可以關掉終端機視窗了，
**以後都不用再開終端機**。

## 步驟 5：確認一下

桌面上應該多了兩個東西：「📝 產生逐字稿」和「🔄 更新工具」。
建議先拿一段 5 分鐘的錄音測一次，確認整條路都通。

有空的話打開 `bin/人名對照表.txt`（在「文件 → meeting-transcribe → bin」裡），
把常出現的同事、客戶名字加進去，之後就不會一直聽錯。

---

# 第二部分：日常使用（每次只要三步）

1. 雙擊桌面的「📝 產生逐字稿」
2. 選錄音檔
3. 等它跑完，逐字稿會自動打開

結果存在「文件 → meeting-transcribe → 逐字稿」資料夾，一份 `.txt`（給人看）和一份 `.srt`（字幕檔）。

## 以後要更新版本

以後如果有新版本，只要雙擊桌面的「🔄 更新工具」，等它跑完就好，
不用重跑「安裝.command」，你改過的 `bin/人名對照表.txt` 也不會不見。

---

## 大概要等多久

在 M1 / 16GB 的機器上，**1 小時的會議大約要 45～75 分鐘**。轉錄期間電腦可以照常用，
只是會比較燙、比較慢。建議開會結束後按下去，去做別的事。

想更快可以改用小一點的模型（準確度會降一些）：打開 `bin/transcribe.py`，
把 `large-v3-turbo` 改成 `medium` 或 `small`。

## 檔案在哪

| 東西 | 位置 |
|---|---|
| 逐字稿成品 | 文件 → meeting-transcribe →「逐字稿」資料夾 |
| 人名對照表 | 文件 → meeting-transcribe → `bin/人名對照表.txt` |
| Python 環境和模型 | `~/Library/Application Support/MeetingTranscribe/` |
| 授權碼 | `~/Library/Application Support/MeetingTranscribe/hf_token.txt` |

要完全移除：刪掉 `~/Library/Application Support/MeetingTranscribe/`、
`~/.cache/huggingface/`、桌面上「📝 產生逐字稿」與「🔄 更新工具」的捷徑，和 meeting-transcribe 這個資料夾。

---

## 出問題時

| 狀況 | 處理 |
|---|---|
| 步驟 2 打完說 `command not found: git` | 貼上 `xcode-select --install` 按 Enter，按「安裝」，裝完再試一次 |
| 步驟 3 說 `already exists and is not an empty directory` | 之前已經下載過了。改貼 `bash ~/Documents/meeting-transcribe/安裝.command` 按 Enter |
| 步驟 3 卡在 `Cloning into…` 很久 | 網路慢或斷線。按 Ctrl+C 中斷，確認網路後整段重貼一次 |
| 雙擊沒反應／說「無法打開」 | 開「終端機」，輸入 `bash` 加一個空格，把該檔案從 Finder 拖進去按 Enter |
| 說「還沒安裝」 | 重跑一次「安裝.command」 |
| 逐字稿沒有講者標記 | 授權碼沒設好，或忘了在網頁按同意。重跑「安裝.command」 |
| 跑到一半卡住不動超過 10 分鐘 | 那段可能是長靜音。關掉視窗，用剪輯軟體把前後靜音剪掉再試 |
| 人名一直錯 | 加到 `bin/人名對照表.txt`，下一份就會自動改對 |
| 出現簡體中文 | 重跑一次「安裝.command」，讓繁簡轉換套件裝進去 |
| 「🔄 更新工具」跑失敗 | 檢查有沒有連網，再試一次；還是不行請截圖給幫你安裝的人看 |

## 技術說明

- 轉文字：`faster-whisper` + `large-v3-turbo`，int8 量化，純 CPU
- 輸出前用 OpenCC（s2twp）轉成台灣繁體，避免 Whisper 預設的簡體（軟件→軟體、信息→資訊）
- 講者辨識：`pyannote/speaker-diarization-community-1`
- 音檔解碼走 PyAV（wheel 自帶 ffmpeg），波形以記憶體傳給 pyannote，
  因此**不需要另外安裝 Homebrew 或 ffmpeg**
- git 來自 Apple 官方的 Xcode Command Line Tools（`xcode-select --install`），同樣不需要 Homebrew
- 兩個模型序列執行、中間釋放，峰值記憶體約 2GB，16GB 機器很寬裕
- Python 環境由 `uv` 建立在獨立目錄，不會動到系統 Python
